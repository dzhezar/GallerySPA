<div class="row py-2-lg">
    <div class="responsive-menu">
        <div class="responsive"><i class="fas fa-bars pr-3"></i></div>
        <p class="logo"><a href="{{ path('index') }}">wojciech</a></p>
        <ul class="nav nav-menu justify-content-around">
            {% for category in categories %}
            <li class="nav-item"><a href="{{ path('portfolioCategory',{category: category.slug}) }}">{{ category.name }}</a></li>
            {% endfor %}
{#            <li class="nav-item"><a href="#about">About me</a></li>#}
{#            <li class="nav-item"><a href="#contact">Contact</a></li>#}
        </ul>
    </div>
</div>