{% extends 'admin/base.html.twig' %}

{% block body %}
    <div class="card-body">
        {{ form_start(form) }}
        {{ form_row(form.facebook) }}
        {{ form_row(form.instagram) }}
        {{ form_row(form.mail) }}
        {{ form_row(form.tumblr) }}
        {{ form_row(form.mainImg) }}
        Current:
        <img style="max-width: 300px;" src="/uploads/indexImages/{{ main_img }}">
        {{ form_row(form.aboutMe) }}
        {{ form_end(form) }}
    </div>

{% endblock %}

